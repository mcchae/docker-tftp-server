FROM alpine:latest
MAINTAINER Jerry <mcchae@gmail.com>

RUN apk update && apk add --no-cache tftp-hpa tcpdump && \
    mkdir -p /data

EXPOSE 69/udp
VOLUME /data

ENTRYPOINT ["in.tftpd"]

CMD ["-L", "-vvv", "-u", "root", "--secure", "--create", "/data"]
